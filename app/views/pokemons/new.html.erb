<h1> Create New Pokemon! </h1>
<%= simple_form_for Pokemon.new, url: {action: "create"} do |f| %>
  <%= f.input :name, required: true, label: 'Pokemon Name' %>
  <%= f.button :submit %>
<% end %>
